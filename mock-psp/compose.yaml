services:
  mock-psp:
    build: .
    ports:
      - "9090:9090"
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsS http://localhost:9090/admin/chaos || exit 1" ]
      interval: 5s
      timeout: 3s
      retries: 10
